{
  "name": "nova-clothing",
  "version": "1.0.0",
  "private": true,
  "description": "Static NOVA Clothing storefront ready for GitHub, Vercel, and Netlify.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node scripts/check-assets.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
